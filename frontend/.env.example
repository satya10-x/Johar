# Base URL of the JOHAR backend API (no trailing slash).
# Leave empty in development to use the Vite dev-server proxy (/api -> localhost:4000).
VITE_API_BASE_URL=

# Production example:
# VITE_API_BASE_URL=https://johar-api.onrender.com/api
